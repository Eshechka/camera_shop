export enum AppRoute {
  Root = '/',
  Basket = '/cart',
  Product = '/product',
  NotFound = '/404',
}

export enum APIRoute {
  Products = '/cameras',
  Reviews = '/reviews',
  Similar = '/similar',
  Promo = '/promo',
}

export enum NameSpace {
  Product = 'PRODUCT',
  Catalog = 'CATALOG',
}
